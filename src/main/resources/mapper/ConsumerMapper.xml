<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.y261.dao.ConsumerMapper">

    <!-- 通用结果映射 -->
    <resultMap id="BaseResultMap" type="com.y261.entity.Consumer">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="sex" property="sex"/>
        <result column="phone_num" property="phoneNum"/>
        <result column="email" property="email"/>
        <result column="birth" property="birth"/>
        <result column="introduction" property="introduction"/>
        <result column="location" property="location"/>
        <result column="avator" property="avator"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 通用列 -->
    <sql id="Base_Column_List">
        id, username, password, sex, phone_num, email, birth,
        introduction, location, avator, create_time, update_time
    </sql>
    <delete id="deleteById" parameterType="java.io.Serializable">
        DELETE FROM consumer WHERE id = #{id}
    </delete>

    <!-- 根据用户名查询 -->
    <select id="selectByUsername" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT
        <include refid="Base_Column_List"/>
        FROM consumer
        WHERE username = #{username}
        LIMIT 1
    </select>

    <!-- 根据手机号查询 -->
    <select id="selectByPhoneNum" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT
        <include refid="Base_Column_List"/>
        FROM consumer
        WHERE phone_num = #{phoneNum}
        LIMIT 1
    </select>

    <!-- 登录校验 -->
    <select id="selectForLogin" resultMap="BaseResultMap" parameterType="java.lang.String">
        SELECT
        <include refid="Base_Column_List"/>
        FROM consumer
        WHERE username = #{username}
          AND password = #{password}
        LIMIT 1
    </select>

</mapper>